const CommonComponents = require('ui-components/scripts/main');
const userConfig = require('user-config');

module.exports = CommonComponents.settings({
  // Project settings can be defined here
}, userConfig);
